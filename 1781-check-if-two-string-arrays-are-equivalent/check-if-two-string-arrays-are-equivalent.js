/**
 * @param {string[]} word1
 * @param {string[]} word2
 * @return {boolean}
 */
var arrayStringsAreEqual = function(word1, word2) {
    // for(let i = 0;i<word1.length;i++){
        
    // }
    let arr= word1.join("")
    let arr1= word2.join("")
    if(arr==arr1){
        return true
    }else{
        return false
    }
    // console.log(arr)
};